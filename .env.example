# Google Maps API Configuration
# Get your API keys from: https://console.cloud.google.com/

# Google Maps API Key (for Maps SDK, Directions API, Distance Matrix API)
# Use a restricted key (referrer/app restriction + API restriction) and keep it out of Git.
# REQUIRED if EXPO_PUBLIC_GOOGLE_MAPS_PROXY_URL is not set
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=YOUR_GOOGLE_MAPS_API_KEY

# Google Places API Key (for Places API, Autocomplete)
# Use a restricted key (referrer/app restriction + API restriction) and keep it out of Git.
# REQUIRED if EXPO_PUBLIC_GOOGLE_MAPS_PROXY_URL is not set
EXPO_PUBLIC_GOOGLE_PLACES_API_KEY=YOUR_GOOGLE_PLACES_API_KEY

# Google Maps Proxy URL (optional, recommended for production)
# If set, all Google Maps web-service API calls will go through this proxy.
# If not set, the app will make direct calls to Google APIs using the API keys above.
# See: https://developers.google.com/maps/api-security-best-practices
# EXPO_PUBLIC_GOOGLE_MAPS_PROXY_URL=https://your-proxy.example.com
# EXPO_PUBLIC_GOOGLE_MAPS_PROXY_TOKEN=your-proxy-auth-token

# QR Code Signing Secret (for trip verification)
# Generate a random string and keep it secret. Rotate periodically for security.
# Example: openssl rand -base64 32
EXPO_PUBLIC_QR_SECRET=YOUR_RANDOM_SECRET_STRING

# Environment (development or production)
NODE_ENV=development
